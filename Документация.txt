меню в качестве case (1-решение на "3"; 2-решение на "5"; 0-выход из программы)

3:
-Введите номер группы. (ограничения на группу [если grep не находит ничего], иначе предупреждение об ошибке и возврат(цикл while))
-Выбор действия
--Пройтись по обоим предметам, найти человека, у которого есть 2 за тест. (grep -r (;2$) ./labfiles/*/tests/TEST-*)
--Вывод списка группы только фамилии + кол-во попыток (массивы)

4:
-Выбор действия
--Ввод фамилии[цикл while, если grep не находит ничего, то выдать предупреждение и возврат](уточнить, что по БАРСУ(ФИО)); Grep (по фамилии+оценка 3-5), буферная переменная, с которой будет суммироваться оценка,которая потом делиться на 4 и выводится

##Если учитывать неудачные попытки то, Grep (по фамилии), буферная переменная, с которой будет суммироваться оценка,которая потом делиться на n(grep -с для подсчета попыток всего) и выводится
--Ввод группы
---grep по группе -с [ .[$i]1.*$], заносим в массив; ищем наибольшее/наименьшее по массиву и выводим, где i+1 - номер занятия